extends layouts/main-layout.pug

block styles
     link(rel="stylesheet", href="/css/shop.css")

block content
    if prods.length >0
        .grid
            each product in prods
                article
                    header
                        h1 #{product.title}
                    div
                        h2 $19.99
                        p Des......
                    div
                        button Add to Cart 
    else
        h1 No Products
                
